apiVersion: v1
kind: Service
metadata:
  name: demo-svc
  namespace: demo
spec:
  type: ClusterIP
  selector:
    app: demo          # ❌ WRONG (pods are app=demo-app)
  ports:
    - name: http
      port: 80
      targetPort: 80   # ❌ WRONG (container listens on 8080)